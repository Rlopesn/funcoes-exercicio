
const soma = (num1, num2) => {
    const resultadoSoma = num1 + num2
    return (`valor da soma é: ${resultadoSoma}`)
}
console.log(soma(2,9))

const subtração = (num1, num2) => {
    const resultadoSub = num1 - num2
    return (`valor da subtração é: ${resultadoSub}`)
}
console.log(subtração(13, 3))

const divisao = (num1, num2) => {
    const resuldadoDiv = num1 / num2
    return (`valor da divisão é: ${resuldadoDiv}`)
}
console.log(divisao(12, 6))

const multiplicacao = (num1, num2) => {
    const resultadoMult = num1 * num2
    return (`valor da multiplicação é: ${resultadoMult}`)
}
console.log(multiplicacao(12, 3))
 
let num1
let pergNumb = "insira um numero"
num1 = Number(prompt(pergNumb))
console.log(num1)

let num2
num2 = Number(prompt(pergNumb))
console.log(num2)

soma
    const retornoSoma = (num1 + num2)
    console.log(`valor da soma do usuario é: ${retornoSoma}`)

subtração
    const retornoSub = (num1 - num2)
    console.log(`valor da subtração do usuario é: ${retornoSub}`)

divisao
    const retornoDiv = (num1 / num2)
    console.log(`valor da divisão do usuario é: ${retornoDiv}`)

multiplicacao
    const retornoMult = (num1 * num2)
    console.log(`valor da multiplicação do usuario é: ${retornoMult}`)
